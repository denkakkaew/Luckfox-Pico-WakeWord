# Generated artifacts (regenerated by the pipeline): tflite, rknn, labels,
# dataset.txt, calib/. Whole dir is generated.
artifacts/

# Dataset downloads (demo set ~180 MB)
data/

# Board build outputs (kept under board/; sources kws.c + Makefile are tracked)
board/kws
board/*.o

# External SDKs (cloned into the devcontainer volume, never into the repo)
luckfox-pico/
rknn-toolkit2/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Editor / OS
.DS_Store
